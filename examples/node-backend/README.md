# Node Backend Example

This example will show an Express/Fastify-style backend integration using `@iota-gaskit/sdk`.

The backend owns the app credential, forwards policy-relevant metadata, and returns only safe reservation/execution data to the frontend.
