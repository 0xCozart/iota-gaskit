# Next.js API Route Example

This example will show how a dApp backend can keep app credentials server-side while exposing safe transaction sponsorship endpoints to its frontend.

Planned endpoints:

- `POST /api/gaskit/reserve`
- `POST /api/gaskit/execute`

The frontend should never receive the app API key.
